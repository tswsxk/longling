from setuptools import setup

test_deps = [
    'pytest>=4',
    'pytest-cov>=2.6.0',
    'pytest-flake8',
    'flake8<5.0.0'
]

setup(
    name=$PROJECT,
    version='0.0.1',
    extras_require={
        'test': test_deps,
    },
    install_requires=[
    ],  # And any other dependencies for needs
    entry_points={
    },
)
