language: python

matrix:
  include:
    - python: 3.4
    - python: 3.5
    - python: 3.6
    - python: 3.7
      dist: xenial
      sudo: true
  
install:
  - pip install -e .[test,ml-full]
  - pip install codecov

script:
  - pytest
  - codecov
